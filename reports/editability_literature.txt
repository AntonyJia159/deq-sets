================================================================================
EDITABILITY / MAINTAINABILITY OF (GRAPH) NEURAL NETWORKS -- LITERATURE RECORD
Maintained for the DEQ-sets project. Last updated 2026-06-29.
Question we are positioning against: after a GRAPH EDIT (node/edge/feature
deletion), how do you cheaply and correctly UPDATE a trained model's outputs,
and when is the effect of the edit LOCAL? Our angle: an implicit/equilibrium
model's fixed point is EDIT-LOCAL exactly when (I-J) is well-conditioned
(sigma_min(I-J) bounded away from 0), by resolvent decay (Demko-Moss-Smith /
Benzi-Golub); then warm-start + local re-solve maintains it. Below: who has
done what, and where the gap is.
================================================================================

TL;DR MAP OF THE FIELD (five clusters; our gap in CAPS at the end of each)
  1. Graph UNLEARNING / certified removal  -> goal is FORGETTING (privacy), not
     cheap correct maintenance. Influence-function / Newton-step updates.
     GAP: NOT about equilibrium models; locality of the edit is assumed/bounded
     per-method, never tied to a CONDITION NUMBER of a fixed-point map.
  2. Editable GNNs / model editing          -> goal is CORRECTING mispredictions
     while not disturbing others ("locality" metric). EGNN freezes propagation.
     GAP: edits the WEIGHTS to fix outputs; not GRAPH edits, not equilibrium.
  3. Dynamic-graph incremental inference     -> InstantGNN updates only AFFECTED
     nodes after each edge event. THE CLOSEST PRECEDENT -- but LINEAR (PPR).
     GAP: linear propagation only; no nonlinear equilibrium; no conditioning
     characterization of WHY the update stays local.
  4. Implicit / equilibrium GNNs            -> IGNN/IDGNN use fixed points for
     LONG-RANGE expressivity; well-posedness via Perron-Frobenius contraction.
     GAP: locality is treated as a LIMITATION to overcome (they WANT global
     reach), never as a maintainability FEATURE; no edit/warm-start story.
  5. Numerical foundations                   -> resolvent/inverse decay (Demko,
     Benzi-Golub) and low-rank update (Sherman-Morrison-Woodbury) are exactly
     our machinery, but unconnected to GNN editing in the ML literature.
     GAP: nobody imports DMS/SMW to certify+exploit edit-locality of a NONLINEAR
     graph equilibrium.

OUR ONE-LINE POSITION: the nonlinear-equilibrium generalization of InstantGNN,
with a CONDITIONING-BASED (sigma_min(I-J)) characterization of when warm-start
local re-solve is valid -- i.e. resolvent-decay editability for implicit GNNs.

--------------------------------------------------------------------------------
1. GRAPH UNLEARNING / CERTIFIED REMOVAL
--------------------------------------------------------------------------------
GNNDelete (Cheng et al., ICLR 2023, arXiv:2302.13406)
  - Model-agnostic LAYER-WISE deletion operator inserted AFTER GNN layers; only
    updates representations of nodes in the local enclosing subgraph of the
    deleted element. Two objectives: Deleted Edge Consistency (remove influence)
    + Neighborhood Influence (preserve the rest).
  - 12.3x faster / 9.3x less memory than retraining; up to +38.8% AUC vs prior.
  - RELEVANCE: "only update the local subgraph" = a locality assumption baked in
    by construction. We would instead DERIVE the locality radius from sigma_min.

GIF: Graph Influence Function (Wu et al., 2023, arXiv:2304.02835)
  - Influence-function unlearning for feature/node/edge removal; closed-form-ish
    parameter update estimating the effect of removing data.
Certified Edge Unlearning / CEU (Wu et al., KDD 2023)
  - Closed-form influence-function update on parameters for edge removal, with
    (eps,delta) theoretical removal bounds; accounts for the removed edge's nbhd.
IDEA (arXiv:2407.19398) ; Certified Signed Graph Unlearning (ICLR 2026,
  arXiv:2511.14168) ; "Graph Unlearning: A Review" survey.
  - Theme: Newton/influence updates + propagation-aware Hessian, certified
    (eps,delta). Local propagation BOUNDS appear, but as privacy guarantees.
  WHERE WE DIFFER: unlearning's goal is to make the model behave as if data were
  never seen (forgetting). Ours is correctness+cost of MAINTENANCE under graph
  change, and the central object is the fixed-point resolvent, not the Hessian.

--------------------------------------------------------------------------------
2. EDITABLE GNNs / MODEL EDITING (and the editing metrics we should adopt)
--------------------------------------------------------------------------------
EGNN: Editable Graph Neural Network for Node Classification
  (Liu et al., 2023, arXiv:2305.15529)
  - Corrects mispredictions by STITCHING an MLP onto a frozen GNN; DISABLES
    neighbor propagation during editing (propagation is the source of editing
    instability / non-locality) but keeps it for prediction.
  - Insight we can cite: propagation makes edits NON-LOCAL & unstable -> they
    avoid it; we instead show a WELL-CONDITIONED equilibrium keeps propagation
    AND stays local.
Gradient Rewiring for Editable GNN Training (arXiv:2410.15556).
Knowledge-editing metric triple (from the LLM model-editing literature; e.g.
  "A Survey on Knowledge Editing of Neural Networks", arXiv:2310.19704):
    RELIABILITY  - the edit takes (edited input gives intended output)
    GENERALITY   - it generalizes to equivalent inputs
    LOCALITY     - unrelated inputs are UNCHANGED   <-- our "edit-locality"
  ADOPT THIS VOCABULARY: our "far-field prediction unchanged" = their LOCALITY
  metric; "label-exact local re-solve" = their RELIABILITY. Frames our work in
  the established editing-evaluation language.

--------------------------------------------------------------------------------
3. DYNAMIC-GRAPH INCREMENTAL INFERENCE  (the closest precedent)
--------------------------------------------------------------------------------
InstantGNN (Zheng et al., KDD 2022)  *** CLOSEST PRIOR WORK ***
  - Maintains approximate Personalized-PageRank propagation; on each edge event
    updates ONLY the affected nodes locally; impact on the rest is carried by
    the next propagation step. Amortized O(1)-ish per event; structural+feature
    updates; adaptive head retrain only when accumulated drift crosses a thresh.
  - THIS IS THE LINEAR VERSION OF OUR IDEA. PPR = (I - (1-a)P)^{-1} applied to a
    personalization vector: a linear resolvent. They exploit its locality
    implicitly. WE GENERALIZE TO A NONLINEAR EQUILIBRIUM z=f(z) and make the
    locality condition EXPLICIT (sigma_min(I-J)), with warm-start Broyden as the
    update. Must cite + benchmark against (the linear baseline to beat).
DyGRAIN (IJCAI 2022) - incremental LEARNING on dynamic graphs (catastrophic
  forgetting under time-varying receptive fields); about training, not inference.
Incrementally-Computable Neural Networks (arXiv:2307.14988)
  - Vector-quantize intermediate activations so small input edits reuse cached
    values; ~12x fewer ops for streams of atomic edits. General NN, not graphs;
    a COMPLEMENTARY engineering trick (value reuse) to our equilibrium warm-start.
"A survey of dynamic graph neural networks" (arXiv:2404.18211) - taxonomy ref;
  note their framing: incoming nodes/edges change receptive fields and can flip
  predictions under fixed params (the phenomenon our edit-locality bounds).
  BOUNDARY (from our own notes): dynamic-GNN lit assumes the graph is genuinely
  EVOLVING (path-dependent history matters); we assume a STATIC graph being
  edited (path-independent; the fixed point is a pure function of the current
  graph). Different problem, related machinery.

--------------------------------------------------------------------------------
4. IMPLICIT / EQUILIBRIUM GRAPH NEURAL NETWORKS
--------------------------------------------------------------------------------
IGNN: Implicit Graph Neural Networks (Gu et al., NeurIPS 2020, arXiv:2009.06211)
  - Prediction = fixed point Z* = phi(A Z* W + U(X)); well-posedness requires
    Perron-Frobenius lambda_pf(|A (x) W|) <= 1 (a CONTRACTION condition).
  - Sells the fixed point for LONG-RANGE dependence / no over-smoothing -- i.e.
    they want GLOBAL reach. We point out reach = edit-length = xi (same
    resolvent), so their global-reach regime is exactly the NON-maintainable one;
    the maintainable regime is short-xi (well-conditioned, NOT contraction-tight).
IDGNN: Efficient & Effective Implicit DYNAMIC GNN (arXiv:2406.17894)
  - Implicit model for dynamic graphs; contribution = recast training as bilevel
    optimization w/ moving averages -> up to 1600x training speed-up. "First
    implicit NN for dynamic graphs."
  - CHECKED: NOT about warm-start edit maintenance, NOT edit-locality, NOT
    conditioning/resolvent. Their "dynamic" = aggregate over time during
    TRAINING. Name-collision risk but DIFFERENT contribution. Distinguish clearly.
IGNN-Solver (arXiv:2410.08524) - learns a small GNN to accelerate the fixed-point
  solve (Anderson), 1.5-8x faster inference. Orthogonal (faster solve); could be
  combined with our warm-start.
Implicit vs Unfolded GNNs (Yang et al., arXiv:2111.06592) - directly relevant to
  our equilibrium-vs-finite-depth question; compares implicit equilibria to
  finite-depth unfolded nets (expressivity/robustness). [PDF wouldn't parse; FETCH
  HTML/abstract next session -- need their exact claim on when the two differ.]
TorchDEQ (arXiv:2310.18605) - the library we use. RevDEQ (arXiv:2509.12917) -
  exact reversible gradients, constant memory (an alternative to implicit-diff).

--------------------------------------------------------------------------------
5. NUMERICAL FOUNDATIONS (our actual machinery; absent from the GNN-edit lit)
--------------------------------------------------------------------------------
Demko, Moss & Smith (1984) - inverse of a banded SPD matrix decays exponentially
  off-diagonal; rate set by bandwidth + extreme eigenvalues (=> condition number).
Benzi & Golub; Benzi & Razouk "Bounds for the entries of matrix functions..."
  (cs.emory.edu/~benzi) - generalize decay to f(A) and to graph-sparse A (decay
  in GEODESIC graph distance). THIS is the bound we use: |(I-J)^{-1}_ij| <=
  C q^{d(i,j)}, q=(sqrt(kappa)-1)/(sqrt(kappa)+1), kappa=kappa(I-J).
  -> screening length xi ~ sqrt(kappa)/2. (Localization of matrix factorizations,
  arXiv:1305.1618, for the factorization analog.)
Sherman-Morrison-Woodbury (1950s) - exact inverse update under a LOW-RANK
  modification B = A + P Q^T. A graph edit (delete edge/node) IS a low-rank +
  sparse modification of (I-J). Survey of low-rank preconditioner updates
  (arXiv:2406.15120 and refs). RELEVANCE: gives an alternative EXACT local-update
  route (vs Broyden warm-start) and a principled cost model for the edit.

--------------------------------------------------------------------------------
SYNTHESIS / WHERE THE WHITE SPACE IS
--------------------------------------------------------------------------------
* The model-editing world has the right METRICS (reliability/generality/
  locality) but edits WEIGHTS of feedforward nets.
* The unlearning world has CERTIFIED local updates but for FORGETTING via
  influence functions, not fixed-point maintenance.
* The dynamic-graph world (InstantGNN) has LOCAL incremental inference but only
  for LINEAR propagation (PPR resolvent).
* The implicit-GNN world has the FIXED POINT but uses it for GLOBAL reach and
  proves only contraction well-posedness; treats locality as a limitation.
* The numerical world has the exact decay+update theory (DMS/Benzi-Golub/SMW)
  but it has not been imported to GNN editing.

UNOCCUPIED INTERSECTION (our claim):
  "An implicit/equilibrium GNN's fixed point is EDIT-LOCAL iff (I-J) is
   well-conditioned (sigma_min(I-J) away from 0, NOT requiring contraction);
   then a graph edit can be maintained by warm-start local re-solve, with a
   provable screening length xi ~ sqrt(kappa)/2 and far-field invariance
   (the 'locality' editing metric). This is the nonlinear-equilibrium
   generalization of InstantGNN, justified by Demko-Moss-Smith/Benzi-Golub."

--------------------------------------------------------------------------------
MAINTENANCE TAXONOMY (4-way) -- expressivity vs how you maintain after an edit
--------------------------------------------------------------------------------
Split the candidate models by HOW an edit is propagated. Reach = edit-radius = xi
for ALL of them (same resolvent, truncated or full); the decay within the reach
ball is governed by conditioning in every case. What differs is the MECHANISM.

(1) DECOUPLED-LINEAR  [InstantGNN, PPRGo, SGC, APPNP, SIGN]
    form: linear propagation A^k X, nonlinearity OFF TO THE SIDE (node-wise MLP
    before or after ALL propagation; propagation chain stays linear).
    maintain: CHEAP EXACT INCREMENTAL -- maintain residuals, PUSH only the delta
    (PPR/forward-push invariants rely on linear superposition).
    cost: ~size of push frontier; exact. EXPRESSIVITY: WEAK (operator family R^2
    0.0-0.33; cannot interleave nonlinearity with propagation).

(2) INTERLEAVED FINITE-DEPTH  [K-layer GCN/GIN; also STACKED SIGN = multi-hop per
    layer -- NOT a 4th class, just fatter layers in this same family]
    form: sigma(A sigma(A ... x)) -- nonlinearity BETWEEN every propagation.
    maintain: LOCAL RECOMPUTE ONLY. The nonlinearity is a BARRIER: you cannot push
    through an MLP. So it is L separate push-phases with a MATERIALIZE step between
    (push linear block -> recompute node-MLP on the changed frontier -> push next
    block, frontier grows ~h hops/layer). Must STORE every intermediate-layer state.
    cost: ~(L*h)-hop ball, with bespoke per-layer residual bookkeeping = "editable
    but UNWIELDY". EXPRESSIVITY: STRONG (matches the equilibrium).
    Also: naive depth is UNSTABLE (GIN collapse 0.20@K2 -> 0.07@K3, oversmoothing).

(3) EQUILIBRIUM  [ours: well-conditioned nonlinear DEQ/IGNN-class]
    form: one fixed point z=f(z); f interleaves propagation+nonlinearity, run to
    convergence (a weight-tied interleaved net at infinite/adaptive depth).
    maintain: WARM-START LOCAL RE-SOLVE -- start from old z*, perturb, re-solve;
    the solver propagates through the nonlinearity BY ITERATING; warm-start => few
    iters (only the xi-ball is far from solution); IFT => O(1) MEMORY (no stored
    intermediate states); reach xi set by sigma_min, not hand-tuned depth K.
    cost: ~xi-ball; SAME complexity class as (2) but ONE object, no per-layer
    bookkeeping. EXPRESSIVITY: STRONG (= interleaved).

UNIFYING VIEW: a weight-tied interleaved net run to convergence IS the equilibrium;
finite-depth interleaved is a TRUNCATED equilibrium; decoupled-linear is the LINEAR
resolvent (bonus: exact delta-push). The SAME resolvent-decay theory + the single
knob sigma_min(I-J) explains the edit-locality of ALL THREE. So:
  * vs (1): we WIN ON EXPRESSIVITY (editability previously REQUIRED linearity; we
    show conditioning, not linearity, is the enabler -> editable class enlarged to
    interleaved-nonlinear/signed/anisotropic).
  * vs (2): expressivity TIES; our edge is MECHANISM -- single local re-solve vs
    L-layer push-and-materialize bookkeeping, + O(1) memory + adaptive xi +
    stability. NOT a categorical maintenance win, but a cleaner one.
  * the THEORY (sigma_min characterization) covers the WHOLE family -- the science.
DROP any "beyond-finite-depth expressivity" claim; it is false (unroll K~=reach
matches the equilibrium).

--------------------------------------------------------------------------------
6. IMPLICIT MODELS x NEURAL ALGORITHMIC REASONING (scanned 2026-06-29) -- the
   EXPRESSIVITY/EXECUTION angle is ALREADY OCCUPIED, incl. tropical
--------------------------------------------------------------------------------
DEAR -- Deep Equilibrium Algorithmic Reasoner (Cnops/Numeroso/Velickovic?,
  arXiv:2402.06445, 2024): trains a DEQ to find the fixed point that an
  ALGORITHM's solution represents -- EXPLICITLY Bellman-Ford, Floyd-Warshall
  (= tropical/max-plus shortest path), SCC, insertion sort (CLRS-30). Insight:
  "once the optimum is found, further iterations don't change the output" => it's
  an equilibrium. *** So "a DEQ can compute a tropical/algorithmic equilibrium"
  is NOT NOVEL -- DEAR did it. *** BUT: STATIC instances only; NO edit-locality,
  NO maintenance/warm-start, NO conditioning, NO semiring-as-design-axis.
Neural Algorithmic Reasoners are Implicit Planners (Deac et al., NeurIPS 2021,
  arXiv:2110.05442) + XLVIN (arXiv:2010.13146): VALUE ITERATION (max-plus
  planning) as an implicit/recurrent reasoner. Tropical-equilibrium-for-planning
  also taken. (Lineage: Value Iteration Networks, Tamar 2016.)
IterGNN (arXiv:2010.13547): adaptive/unbounded-depth iterative GNN, universal
  approx of iterative algorithms, scale-generalization. EIGNN (arXiv:2202.10720):
  efficient infinite-depth implicit GNN. Both = expressivity/depth, not maintenance.
POSITIONING CONSEQUENCE (load-bearing, decide before the Geng smell-check):
  * DO NOT sell "DEQs can do tropical / algorithmic operators" -> DEAR's turf.
  * UNOCCUPIED LAND = edit-locality + MAINTENANCE + the sigma_min characterization
    (DEAR explicitly: "static instances rather than evolving graphs").
  * Demote tropical to a SPAN-PROOF ("the maintenance theory spans semirings"),
    cite DEAR/VIN so we don't overclaim. Geng Q sharpens to: "given DEAR owns
    implicit-algorithmic-EXECUTION, is implicit-algorithmic-MAINTENANCE a paper?"

--------------------------------------------------------------------------------
PAPER-READINESS GAPS (the experiment section is ~40% built; theory is solid)
--------------------------------------------------------------------------------
  [1] RING-TRUNCATION is the #1 gap (ZJ's flag): currently just a "ring radius"
      number. Needs a HEADLINE FIGURE at SCALE: truncation radius R vs
      (pred error vs exact) vs (wall-clock/op speedup), benchmarked against
      recompute-from-scratch AND real InstantGNN, on a LARGE-DIAMETER graph
      (road net / mesh / large grid; small-world Cora hid the savings).
  [2] SCALE: all <=56x56 grids + small real graphs. Need >=1 large streaming
      real graph (millions of edges, a sequence of edits).
  [3] REAL BASELINES: "InstantGNN-linear" is our linear-cell PROXY, not the
      actual InstantGNN / dynamic-PPR. Benchmark the real ones.
  [4] sigma_min AS A LAW: turn the few dissociation points into a scatter --
      predicted sigma_min/dist(1,W(J)) vs MEASURED xi across many models/tasks.
  [5] SEEDS: 3 -> 5-10 with error bars on headline numbers.

--------------------------------------------------------------------------------
DESIGN-SPACE REFRAME: lead with MAINTENANCE; motivate via ROBUSTNESS
--------------------------------------------------------------------------------
Tropical is weak as a headline (locality<->tropical tension + DEAR pre-empts the
expressivity claim). Stronger framing of the liberalized design space:
  * ROBUSTNESS = the concrete unoccupied use case. Order statistics (median /
    trimmed-mean / max-min) are the textbook robust-to-outliers alternative to the
    MEAN; on a STREAMING graph with noisy/ADVERSARIAL edge churn you want robust
    LOCAL aggregation AND cheap maintenance -- and linear-push incumbents CANNOT
    do robust aggregation (it's a nonlinear order statistic). Combines order-stat
    + local + maintainable + a real application.
  * Robust-statistics DL lineage (descendants of median-not-mean): robust losses
    (Huber, Tukey, Barron-2019 adaptive); order-stat aggregation (max/median pool,
    PNA); BYZANTINE-ROBUST FL aggregation (coordinate-median, trimmed-mean, Krum,
    GEOMETRIC MEDIAN -- Blanchard'17, Yin'18, RFA) <- ties [[federated-direction]];
    distributional/quantile (QR-DQN, Implicit Quantile Nets); DRO/CVaR (worst-case
    = max, tropical-flavoured); gradient clipping (Winsorized grad); sparsemax/
    top-k/MoE routing (soft order-stat selection).
  * TWO clean hooks: (a) geometric-median aggregation = WEISZFELD fixed-point
    iteration => robust aggregation is NATIVELY a DEQ; (b) the aggregator SPECTRUM
    mean -> trimmed/median -> max IS the sum-product -> robust -> tropical axis,
    log-sum-exp/quantile interpolating; only the MEAN end is linear-push-maintainable.
  * "Maintenance for the expressive models people ALREADY deploy" (signed/attention/
    nonlinear), not exotic operators -- bring InstantGNN-style maintenance to the
    expressive class; tropical = the categorical proof it extends past linear.

ROBUSTNESS USE-CASE -- concrete prior art + the experiment to build:
  * PRIOR ART (the robustness half already exists, STATIC): Soft Medoid aggregation,
    Geisler et al. "Reliable GNNs via Robust Aggregation" (NeurIPS 2020) -- replaces
    GCN weighted-MEAN neighbor aggregation with a DIFFERENTIABLE medoid / geometric-
    median-style aggregator, PROVES certifiable robustness to structural attacks.
    Follow-ups: Soft Median, GNNGuard (Zhang & Zitnik 2020). Graph attacks to attack
    WITH: Nettack (Zugner et al. 2018, targeted), Metattack (Zugner & Gunnemann 2019,
    meta-learned global). Federated analog: RFA geometric-median grad aggregation.
  * WHY GNNs are vulnerable: message passing PROPAGATES the attacker -- a few edge
    flips near a target dominate a MEAN aggregate; a median/medoid DOWN-WEIGHTS the
    injected outlier neighbors instead of averaging them in.
  * THE NEW BIT (unoccupied) = the STREAMING/MAINTENANCE half. Soft Medoid is static;
    nobody does robust-AND-cheaply-maintained under adversarial edge churn. The robust
    aggregator is a nonlinear order statistic -> linear-push (InstantGNN/PPR) CAN'T do
    it; recurrentized as an equilibrium it is differentiated by IMPLICIT DIFF (geometric
    median = WEIZFELD fixed point) and maintained by warm-start local re-solve.
  * EXPERIMENT SKETCH: train a geometric-median-aggregation EQUILIBRIUM; subject the
    graph to Nettack/Metattack-style edge injections ARRIVING IN A STREAM; show BOTH
    (i) ROBUSTNESS -- median holds where mean collapses -- and (ii) MAINTAINABILITY --
    warm-start + ring-truncation keeps each update local/cheap as attack edges churn.
    Soft Medoid gives (i); (ii) under a stream of edits is OUR contribution.
  * FRAMING (path-independence -- do NOT call this "dynamic/temporal graphs"): our
    regime is "STATIC GRAPH IN FLUX" -- an edit is a SPURIOUS correction, not signal;
    the fixed point is a pure function of the CURRENT graph (warm-start == cold
    recompute, no hysteresis). This is the OPPOSITE of dynamic/temporal-graph learning
    (TGN etc.), where the edit SEQUENCE is the signal and the model is path-dependent
    by design. Robustness FITS this perfectly: an adversarial/noisy edge IS spurious by
    definition, so we want to be robust to it (median down-weights) AND absorb it locally
    (path-independent re-solve) WITHOUT treating it as information. Right baselines =
    recompute-from-scratch + InstantGNN-style incremental MAINTENANCE (also edit-workload
    regime), NOT temporal-graph-prediction benchmarks. The "scale" gap = a large graph +
    an EDIT WORKLOAD, not a dynamic-graph-learning task.

  * EXPERIMENT RECORD (adv_robustness.py, 2026-06-30): two findings, one honest non-result.
    (1) CONTAINMENT works and is aggregator-agnostic: inject K=2 adversarial edges at a
        victim (connect to most target-OPPOSITE nodes, cheap structural Nettack), warm-start
        re-solve; the prediction shift decays cleanly near-field (1.2e-2 -> 1.1e-5 over 6
        hops, screening length xi ~ 0.71 hops << diameter 62). An adversarial edge is just a
        spurious edit; sigma_min caps its reach. (The far-field uptick is the edge's OTHER
        endpoint lighting its own local ball -- a second equally-contained source.) This is
        the cheap robustness result and needs NO robust aggregator.
    (2) ROBUSTNESS<->CONTRAST TENSION (honest non-result): a geometric-median aggregator (one
        Weiszfeld/IRLS reweight FOLDED into the DEQ iteration -- differentiated natively, no
        nested loop; added as agg="geomedian" in mpnn_deq) CANNOT fit the Laplacian at any
        coupling (R^2 ~ -0.04 up to rho=0.45, vs sum-agg's 0.65). Reason: the geometric median
        is robust BECAUSE it discards neighbourhood spread, but the Laplacian/Dirichlet operator
        IS the spread. So robust aggregation is NOT a free drop-in; pairing it with a contrast
        target gives "robustness by degeneracy" (a constant predictor is trivially un-draggable
        -- an artifact to avoid). Its real payoff is on a CENTRAL-TENDENCY / consensus-denoising
        task (outlier-corrupted node features, drag-the-mean attack), which is the proper next
        build and was deliberately NOT faked here.
    NOTE (infra): hardened mpnn_deq _sn with a CPU-SVD fallback (cuSOLVER gesvdj intermittently
    fails on small matrices), and the trainer now guards non-finite steps (cuda index_add atomics
    are nondeterministic and can tip a boundary-contractive cell into a NaN blow-up over epochs).

OPEN NOVELTY CHECKS (do before claiming):
  [ ] Fetch Implicit-vs-Unfolded (2111.06592) exact equilibrium-vs-finite-depth
      claim -- does anyone already say reach=edit-length for equilibria?
  [ ] Search: "graph condition number" + "editability"/"locality" combined.
  [ ] Search: monotone-operator / non-contractive implicit GNN well-posedness
      (edge-of-well-posedness) -- anyone use sigma_min instead of spectral radius?
  [ ] Geng gut-check: given DEAR owns implicit-algorithmic-EXECUTION, is
      implicit-algorithmic-MAINTENANCE (sigma_min + warm-start + ring-truncation)
      a paper on its own? And: is the robustness use-case the right headline?
================================================================================
